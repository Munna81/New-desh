export const i18n = {
  defaultLocale: "bn",
  locales: ["bn", "en"],
};

export const defaultNS = "common";
export const localePath = "./public/locales";

const config = {
  i18n,
  defaultNS,
  localePath,
};

export default config;
